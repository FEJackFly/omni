syntax = "proto3";
package trunk.msgs;

message Switch {
    string name = 1;
    uint32 command = 2;  // 1: start 2: stop 3: restart
}

message Target {
    message Position {
        double x = 1;
        double y = 2;
    }
    Position destination = 1;
}
